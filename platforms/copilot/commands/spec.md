# spec

Create a new spec for a feature or change using OpenSpec-style structure.

**Usage:** `/ai-dev-team:spec <description>`

Creates spec at `.ai-dev/specs/<change-id>/` with proposal.md, specs/, design.md, and tasks.md.
